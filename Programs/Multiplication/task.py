# Product of all elements of the list
# Author: TODO: ???


def product(a_list):
    """
    >>> product([])
    0
    >>> product([6])
    6
    >>> product([1, 2, 3, 4, 5])
    120
    >>> product([3, -2])
    -6
    >>> product([3, 2.5])
    7.5
    >>> product([4, 5, 6, 0, 8, 9])
    0

    Below tests are to force you to use return instead of print ;-)
    >>> type(product([1])) == int
    True
    >>> type(product([1.1])) == float
    True
    """
    pass  # TODO; body of the function


if __name__ == '__main__':
    import doctest
    doctest.testmod()
